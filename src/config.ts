export const config = {
  /**
   * fetchRandomUser endpoint
   */
  URL: "https://jsonplaceholder.typicode.com/users",
  /**
   * cache is valid for, ms
   */
  cachedValidPeriodMs: 1e5,
};

export default config;
